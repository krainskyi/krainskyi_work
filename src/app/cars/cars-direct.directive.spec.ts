import { CarsDirectDirective } from './cars-direct.directive';

describe('CarsDirectDirective', () => {
  it('should create an instance', () => {
    const directive = new CarsDirectDirective();
    expect(directive).toBeTruthy();
  });
});
